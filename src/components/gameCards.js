import React from 'react';

const GameCards = (props) => {
    return (
        <div className={'bg-green-300'}>
            THIS IS WHERE THE CARDS GOES
        </div>
    )
};

export default GameCards;