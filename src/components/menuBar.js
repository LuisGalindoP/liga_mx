import React from 'react'

const MenuBar = (props) => {
    return (
        <div className={'bg-amber-400 h-12'}>
            THIS IS THE MENU BANNER COMPONENT
        </div>
    )
};

export default MenuBar;