import React from "react";

const NewsCarrousel = (props) => {
    return (
        <div className={'bg-amber-700'}>
            NEW CARROUSEL HERE
        </div>
    )
};

export default NewsCarrousel;